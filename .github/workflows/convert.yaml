name: Create PDF
on: push

jobs:
  lint:
    name: Create PDF
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: mdtopdf-action
        uses: 3rdJCG/mdtopdf-action@0.1.0
        with:
          root_file: README.md
